{
  "name": "markdown-kanban",
  "displayName": "Markdown Kanban",
  "description": "View and manage TODO lists in Markdown files as a Kanban board",
  "version": "0.1.0",
  "publisher": "kyuki3rain",
  "repository": {
    "type": "git",
    "url": "https://github.com/kyuki3rain/markdown-kanban.git"
  },
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:markdown"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "markdownKanban.openBoard",
        "title": "Open Kanban Board",
        "category": "Markdown Kanban",
        "icon": "$(layout)"
      },
      {
        "command": "markdownKanban.openBoardFromEditor",
        "title": "Open Kanban Board",
        "category": "Markdown Kanban",
        "icon": "$(layout)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "when": "resourceLangId == markdown",
          "command": "markdownKanban.openBoardFromEditor",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "markdownKanban.openBoard",
          "when": "resourceLangId == markdown"
        },
        {
          "command": "markdownKanban.openBoardFromEditor",
          "when": "false"
        }
      ]
    },
    "configuration": {
      "title": "Markdown Kanban",
      "properties": {
        "markdownKanban.statuses": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": ["todo", "in-progress", "done"],
          "description": "ステータス一覧（カラムの表示順）"
        },
        "markdownKanban.doneStatuses": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": ["done"],
          "description": "完了扱いとするステータス"
        },
        "markdownKanban.defaultStatus": {
          "type": "string",
          "default": "todo",
          "description": "デフォルトステータス（[ ] の時、status未指定時に使用）"
        },
        "markdownKanban.defaultDoneStatus": {
          "type": "string",
          "default": "done",
          "description": "デフォルト完了ステータス（[x] の時、status未指定時に使用）"
        },
        "markdownKanban.sortBy": {
          "type": "string",
          "enum": ["markdown", "priority", "due", "alphabetical"],
          "default": "markdown",
          "enumDescriptions": [
            "ファイル内の出現順",
            "優先度順",
            "期限順",
            "アルファベット順"
          ],
          "description": "タスクのソート順"
        },
        "markdownKanban.syncCheckboxWithDone": {
          "type": "boolean",
          "default": true,
          "description": "完了ステータス変更時にチェックボックスも連動させるか"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run package",
    "compile": "pnpm --filter @markdown-kanban/core run compile && pnpm --filter @markdown-kanban/webview run build",
    "compile:core": "pnpm --filter @markdown-kanban/core run compile",
    "compile:webview": "pnpm --filter @markdown-kanban/webview run build",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node packages/core/esbuild.js --watch",
    "watch:tsc": "pnpm --filter @markdown-kanban/core run check-types:watch",
    "watch:webview": "pnpm --filter @markdown-kanban/webview run watch",
    "package": "pnpm --filter @markdown-kanban/core run check-types && pnpm --filter @markdown-kanban/core run lint && node packages/core/esbuild.js --production && pnpm --filter @markdown-kanban/webview run build",
    "compile-tests": "node packages/core/esbuild.e2e.js",
    "watch-tests": "node packages/core/esbuild.e2e.js --watch",
    "pretest": "pnpm --filter @markdown-kanban/core run check-types && pnpm --filter @markdown-kanban/core run lint",
    "pretest:e2e": "pnpm run compile-tests && pnpm run compile",
    "check-types": "pnpm --filter @markdown-kanban/core run check-types",
    "lint": "pnpm --filter @markdown-kanban/core run lint",
    "lint:fix": "pnpm --filter @markdown-kanban/core run lint:fix",
    "format": "pnpm --filter @markdown-kanban/core run format",
    "test": "pnpm --filter @markdown-kanban/core run test",
    "test:watch": "pnpm --filter @markdown-kanban/core run test:watch",
    "test:coverage": "pnpm --filter @markdown-kanban/core run test:coverage",
    "test:e2e": "pnpm run pretest:e2e && vscode-test"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.5.0",
    "npm-run-all": "^4.1.5"
  }
}
